#include <iostream>
#include <fstream>
#include <cmath>
using namespace std;

int main()
{
    long long int M,a,b,licznik=0;
   ifstream plik ("liczby.txt");
   for(int i=0;i<1000;i++)
   {
       plik>>M>>a>>b;
      if(nwd())
   }
   cout<<licznik;
    return 0;
}
